# Root requirements.txt — used by Vercel's Python runtime for api/ serverless functions.
# For local backend development see backend/requirements.txt instead.
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
anthropic>=0.35.0
mangum>=0.17.0
openpyxl>=3.1.0

# PyMuPDF is OPTIONAL: if present it is used for higher-fidelity PDF layout.
# pymupdf>=1.24
